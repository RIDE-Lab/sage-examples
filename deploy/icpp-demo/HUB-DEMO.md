# Docker Hub 启动

当前标签 `liujun4hust/sage-icpp-demo:20260926-cpu-full`。

运行 `sh scripts/start-hub.sh` 或 Windows `scripts/start-hub.ps1`。

完整启动、模型配置和实测范围见 [README](README.md)，摘要见 [发布记录](CPU-FULL-PUBLICATION.md)。
